# Install with: 'pip install -r requirements.txt'

# Infrastructure
Django >= 1.6.1, < 1.9
Redis == 2.9.1
gunicorn == 19.3.0
south
PostgreSQL
psycopg2
requests
django-storages
django-celery